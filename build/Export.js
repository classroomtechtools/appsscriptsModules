/**
 * Functions defined here, or any written in the parent directory
 * are put into the project as regular appscripts files (next to appsscript.json)
 */
function myFunction () {
    const {anything} = Import;  // or const anything = Import.anything;
    // do anything
}
